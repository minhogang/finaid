import React, { Component } from 'react';
import { withRouter } from 'react-router-dom';

class Scholarships extends Component {
    render() {
        return <h1>This is the scholarships page</h1>
    }
}

export default withRouter(Scholarships);